function dataTable = make_experiment_data_table()
%MAKE_EXPERIMENT_DATA_TABLE Map AS labels to available experimental R(T) files.
%
% Figures and analysis use AS001--AS006 labels. Some raw-data filenames still
% contain older device IDs; those are kept only inside this data table.
%
% v3 uses the same publication-ready R(T) curve exports as the separate
% "transport plots" project. These CSV files were generated from the selected
% transport traces and are preferable to guessing directly from raw scan files.

curveBase = '/Users/asewaket/Documents/Thesis/Raw Transport Data/transport plots/rt_curves_publication';

dataTable = struct();

dataTable.AS001 = entry('AS001', 'ASD088', ...
    fullfile(curveBase, 'ASD088_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'top_4_10');

dataTable.AS002 = entry('AS002', 'ASD093', ...
    fullfile(curveBase, 'ASD093_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'top_4_10');

dataTable.AS003 = entry('AS003', 'ASD094', ...
    fullfile(curveBase, 'ASD094_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'bottom_3_9');

dataTable.AS004 = entry('AS004', 'ASD087', ...
    fullfile(curveBase, 'ASD087_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'top_4_10');

dataTable.AS005 = entry('AS005', 'ASD051', ...
    fullfile(curveBase, 'ASD051_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'top_4_10');

dataTable.AS006 = entry('AS006', 'ASD092', ...
    fullfile(curveBase, 'ASD092_RT_curve.csv'), ...
    'Publication transport-plot R(T) curve export.', ...
    'top_4_10');

end

function s = entry(asLabel, rawLabel, filePath, note, modelPair)
s = struct();
s.asLabel = asLabel;
s.rawLabel = rawLabel;
s.filePath = filePath;
s.note = note;
s.modelPair = modelPair;
end
